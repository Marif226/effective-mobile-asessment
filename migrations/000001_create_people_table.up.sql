CREATE TABLE people (
    id          int     PRIMARY KEY,
    name        varchar(50),
    surname     varchar(50),
    patronymic  varchar(50),
    age         int,
    sex         varchar(5),
    nationality varchar(50)
);